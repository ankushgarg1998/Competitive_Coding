#include<iostream>
#include<vector>
#include<climits>
using namespace std;
int main()
    {
    int t, n, q, x=100000, i, a1, a2, a3, a0;
    long long int val, min;
    a0 = a1 = a2 = a3 = 100000;
    val = a0 + (a1*x) + (a2*x*x) + (a3*x*x  );
    cout<<val;
    return 0;
    }
